### Ansible - Use Ansible Facts
---
- name: show system facts for hcl
  hosts: all
  become: true

  tasks:
    - name: Print OS info
      debug:
        var: ansible_facts['os_family']

    - name: Print IP address
      debug:
        var: ansible_facts['default_ipv4']['address']

### Terraform — Variables, Outputs, tfvars

#variables.tf
variable "instance_type" {
  default = "c6a.12xlarge"
}

#main.tf
resource "aws_instance" "hclServer" {
  ami           = "ami-0abcd1234abcd1234"
  instance_type = var.instance_type
}

#output.tf
output "hcl_ip" {
  value = aws_instance.hclServer.public_ip
}


### Kubernetes — Node Affinity

apiVersion: v1
kind: Pod
metadata:
  name: hcl-affinity
spec:
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: disktype
                operator: In
                values:
                  - ssd
  containers:
    - name: web
      image: nginx


### Shell Script — Archive Log Files

#!/bin/bash

tar -czf /backup/hcl-logs-$(date +%F).tar.gz /var/log/
echo "Logs archived."


### Jenkins Pipeline — Rollback on Failure

pipeline {
    agent any
    environment {
        INSTITUTE_NAME = "hcl"
    }
    stages {
        stage('Deploy') {
            steps {
                script {
                    try {
                        sh 'echo "Deploying $INSTITUTE_NAME application..."'
                        sh 'exit 1' // simulate failure
                    } catch (Exception e) {
                        echo "Deployment failed. Rolling back $INSTITUTE_NAME application..."
                    }
                }
            }
        }
    }
}

### GitLab CI — Rollback Simulation
You will learn how to **simulate rollback using after_script**.

stages:
  - deploy

variables:
  INSTITUTE_NAME: "hcl"

deploy:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME application..."
    - exit 1
  after_script:
    - echo "Deployment failed. Rolling back $INSTITUTE_NAME application..."