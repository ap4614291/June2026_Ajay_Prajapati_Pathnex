Debug Day


### Ansible — Broken

---
- name: Install nginx
  hosts: all
  become: true 

  tasks:
    - name: missing
      package:
        name: nginx
        state: present


### Terraform — Broken

resource "aws_s3_bucket" "BadBucket" {
  bucket = "pathnex-bucket"
  acl = "public-read"
}


### Kubernetes — Broken

apiVersion: v1
kind: Pod
metadata:
  name: brokenpod
spec:
 containers:
  - name: app
    image: nginx
    ports:
      - containerPort: 80


### Jenkins Pipeline — Automatic Rollback

pipeline {
    agent {
      label 'worker-2'
    }

    environment {
        INSTITUTE_NAME = "Pathnex"
        TEAM = "DevOps"
        ENV = "prod"
    }

    stages {
        stage('Deploy') {
            steps {
                script {
                    try {
                        sh 'echo "Deploying $INSTITUTE_NAME app in $ENV for $TEAM"'
                        sh 'exit 1' // simulate failure to test 
                    } catch (Exception e) {
                        echo "Deployment failed. Rolling back $INSTITUTE_NAME app in $ENV"
                    }
                }
            }
        }
    }
}

### GitLab CI — Rollback Strategy

stages:
  - deploy

variables:
  INSTITUTE_NAME: "Pathnex"
  TEAM = "DevOps"
  ENV = "prod"

deploy:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME app in $ENV for $TEAM"
    - exit 1
  after_script:
    - echo "Deployment failed. Rolling back $INSTITUTE_NAME app in $ENV"